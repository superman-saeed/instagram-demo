import * as actionType  from "../../constants/actionTypes.js";

const explorePhoto=(state=[], action)=>{
  switch (action.type) {
    case actionType.FETCH_EXPLORE_FEED:
          return state;
    default:
         return state;

  }

}


export default explorePhoto;
